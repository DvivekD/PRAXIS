import { VoiceSession } from "../../components/Avatar/VoiceSession";

export default function TestAvatarPage() {
  return (
    <div style={{ height: "100vh", width: "100vw", margin: 0, padding: 0 }}>
      <VoiceSession />
    </div>
  );
}
